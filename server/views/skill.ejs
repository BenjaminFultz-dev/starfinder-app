<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/skill.css">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Play:wght@400;700&display=swap" rel="stylesheet">
    <title>Skills</title>
</head>
<body>
   
        <p>Enter your search paramaters in the corresponding search field for ease of use in-game, OR if skill is missing, fill out the form to add it to the database.</p>
        <div class="form-wrapper">   
        <form method="GET">
                <label for="findSkill">Search for Skill by Name:</label>
                <input type="text" id="findSkill" name="findSkill">
                <input type="submit" class="submit">
           </form>
        </div>
        <h1>Skills</h1>
        <ul>
            <% skills.forEach( el => { %>
                <li class="skill" data-id='<%=el._id%>'>
                    <div class="column">
                        <div class="row">
                            <span class="skills-label">Skill:</span>
                            <span><%= el.skill %></span>
                        </div>
                        <div class="row">
                            <span class="skills-label">Ability:</span> 
                            <span><%= el.ability %></span>
                        </div>
                        <div class="row">
                            <span class="skills-label">Description:</span>
                            <span><%= el.description %></span>
                        </div>
                        <div class="row">
                            <span class="skills-label">Trained Only?</span> 
                            <span><%= el.trainedOnly %></span>
                        </div>
                        <div class="row">
                            <span class="skills-label">Armor Check Penalty?</span> 
                            <span><%= el.armorCheckPenalty %></span>
                        </div>
                    </div>
                </li>
           <% }) %>
        </ul>
        <div class="form-wrapper bottom">
        <h2>Create a Skill</h2>
        <form id="skillForm">
            <div class="form-column">
                <div class="form-row">
                    <label for="skillName">Skill:</label>
                    <input type="text" id="skillName" name="skillName">
                </div>
                <div class="form-row">
                    <label for="skillAbility">Ability:</label>
                    <input type="text" id="skillAbility" name="skillAbility">
                </div>
                <div class="form-row">
                    <label for="skillDescription">Description:</label>
                    <input type="text" id="skillDescription" name="skillDescription">
                </div>
                <div class="form-row">
                    <label for="trainedOnly">Trained Only:</label>
                    <input type="checkbox" id="trainedOnly" name="trainedOnly">
                </div>
                <div class="form-row">
                    <label for="armorCheckPenalty">Armor Check Penalty:</label>
                    <input type="checkbox" id="armorCheckPenalty" name="armorCheckPenalty">
                </div>
                <button class="submit">Submit</button>
            </div>
        </div>
        </form>
    
    <script src="/js/skill.js"></script> 
</body>
</html>